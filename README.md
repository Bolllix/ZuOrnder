# ZuOrnder
Assigns people to beds in rooms by scoring each person–bed pair using rules (e.g., age, gender). It builds a score matrix and uses the Hungarian algorithm to find the optimal assignment maximizing total score.
